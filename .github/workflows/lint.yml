name: lint
on:
  pull_request:
  push:
    branches:
      - master
  schedule:
    # Weekly.
    - cron: "0 0 * * 0"

jobs:
  test:
    strategy:
      matrix:
        python-version:
          - '3.12'
          # unlikely to see any changes in linting between oldest and newest,
          # so just those two will suffice
          - '3.14'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: pip install ruff
      # E701: multiple statements on one line
      # PLW1508: Invalid type for environment variable default
      #   we routinely have things like int(os.getenv('PORT', 1234))
      #   and it's wasteful to use '1234' instead of 1234
      - run: ruff check --ignore E701,PLW1508
